SELECT 
	COMPANY_CD,
	DIST_UNIT_CD,
	COMM_DIST_UNIT_CD
FROM
	FI_DISTUNIT_MST
WHERE
	COMPANY_CD = #{P_COMPANY_CD}
	AND DIST_UNIT_FG = #{P_FIXED_COMBO0}
	AND COMM_DIST_UNIT_CD = #{P_COMM_DIST_UNIT_CD}
ORDER BY DIST_UNIT_CD