SELECT 
    BM.CD_COMPANY,
    BM.CD_BGACCT,
    BM.CD_UPBGACCT,
    BM.NM_BGACCT,
    BM.NM2_BGACCT,
    BM.CD_BGACCTFG,
    BM.LV_BGLEVEL,
    BM.YN_SBGACCT,
    BM.YN_ABGACCT,
    BM.CD_BUNITFG,
    BM.CD_BGLEVEL,
    BM.CD_BCONTERMTP,
    BM.CD_BGMSGST,
    BM.YY_END,
    BM.YN_BFORWD,
    BM.FG_BGACCT2,
    BM.NO_PRN,
    BM.CD_PRN,
    BM.NO_BGPROG,
    BM.CD_BGPROGLEVEL,
    BM.ID_INSERT,
    BM.DTS_INSERT,
    BM.ID_UPDATE,
    BM.DTS_UPDATE,
    CASE WHEN BM.LV_BGLEVEL = '1' THEN 'folder' ELSE 'page' END AS cssClass 
FROM
    FI_BGACCT_MST BM
WHERE
    BM.CD_COMPANY = {@P_CD_COMPANY}
        

